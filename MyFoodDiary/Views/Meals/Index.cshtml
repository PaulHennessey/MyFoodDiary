@model  MyFoodDiary.Models.MealsViewModel

@Scripts.Render("~/bundles/jqueryui")

<link href="@Url.Content("~//Content/themes/vader/jquery-ui-1.10.3.custom.css")" rel="stylesheet" type="text/css" />

<table id="mealTable">
    <tr>
        <th>
        </th>
    </tr>

    @foreach (var meal in Model.Meals)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => meal.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { code = meal.Id })
            @Html.ActionLink("Delete", "Delete", new { code = meal.Id }, new { @class = "DeleteProductLink" })
        </td>
    </tr>
    }
</table>

<br />
<p>
    @Html.ActionLink("Add a new Meal", "Create")
</p>

<script src="@Url.Content("~/Scripts/MyFoodDiaryProducts.js")" type="text/javascript"></script>
